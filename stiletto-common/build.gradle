dependencies {
    compile 'com.google.dagger:dagger:2.5'
}

apply plugin: 'maven'

install {
    repositories.mavenInstaller {
        pom.version = libVersion
        pom.groupId = libGroup
        pom.artifactId = 'stilleto'
    }
}

artifacts {
    archives sourcesJar
    archives javadocJar
}